name: PHP CI with MySQL

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

jobs:
  php-mysql-tests:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: gestion_ecole
        ports:
          - "3306:3306"

    steps:
      - uses: actions/checkout@v3

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          extensions: mbstring, mysqli, pdo_mysql

      - name: Nettoyer le cache Composer et le répertoire vendor # NOUVELLE ÉTAPE
        run: |
          composer clear-cache
          rm -rf vendor
          # Facultatif : Décommentez la ligne suivante si vous voulez régénérer composer.lock
          # rm composer.lock



      - run: composer install --no-interaction --prefer-dist

      - run: php -r "file_exists('.env') || copy('.env.example', '.env');"

      - run: |
          echo "DB_CONNECTION=mysql" >> .env
          echo "DB_HOST=localhost" >> .env
          echo "DB_PORT=3306" >> .env
          echo "DB_DATABASE=gestion_ecole" >> .env
          echo "DB_USERNAME=root" >> .env
          echo "DB_PASSWORD=root" >> .env

      - run: vendor/bin/phpunit
